Dự án: JPTaxi (ITSSJP1-20252-Thu1-JPTaxi)
Mô tả: Hệ thống kết nối tài xế nói tiếng Nhật với người Nhật cần dịch vụ taxi tại Hà Nội.
Phiên bản tài liệu: 1.0
Ngày: 10/05/2026
1. Giới thiệu
Tài liệu này hướng dẫn thiết lập môi trường phát triển cho dự án JPTaxi. Dự án bao gồm:
    - Backend: Xử lý logic, API (đăng ký, đăng nhập, đặt xe...).
    - Frontend: Giao diện người dùng.
    - Database: Lưu trữ dữ liệu người dùng, chuyến đi, tài xế.
Mục tiêu sprint: Hoàn thành cơ sở dữ liệu, môi trường dev, chức năng đăng ký/đăng nhập, xem thông tin tài khoản, và matching chuyến đi.

2. Yêu cầu hệ thống (Prerequisites)
Phần mềm yêu cầu:
    FE: HTML, CSS
    BE: NestJS (Node.js / TypeScript)
    DB: PostgreSQL
    Môi trường phát triển: Tuỳ cá nhân (Visual Studio Code, Eclipse IDE)
    Lưu trữ code: GitHub

3. Cài đặt công cụ cơ bản
3.1. Git
Windows: Tải từ https://git-scm.com/download/win
macOS: brew install git
Linux: sudo apt install git
Cấu hình:
Bash
git config --global user.name "Tên của bạn"
git config --global user.email "email@example.com"
3.2. Node.js (nếu dùng JavaScript/TypeScript)
Tải từ https://nodejs.org (chọn LTS).
Kiểm tra:
Bash
node --version
npm --version
3.3. Docker (Khuyến nghị mạnh)
Tải Docker Desktop: https://www.docker.com/products/docker-desktop/

4. Clone Repository
Bash
git clone https://github.com/AIVIETNAM-AIO-MaNgocThang/ITSSJP1-20252-Thu1-JPTaxi.git
cd ITSSJP1-20252-Thu1-JPTaxi
Cấu trúc thư mục hiện tại:
/backend — API NestJS (TypeScript), cổng mặc định 3000, prefix /api
/frontend — Giao diện HTML/CSS/JS tĩnh (mở file hoặc dùng Live Server)
/database — Script DDL / seed (DB.sql, DB_data.sql)
/docs — Tài liệu hướng dẫn

5. Thiết lập Backend (NestJS)
Bước 1: Vào thư mục backend
Bash
cd backend
Bước 2: Cài dependencies
Bash
npm install
Bước 3: Tạo file môi trường .env
Copy .env.example và chỉnh các biến (tên biến phải khớp code):
env
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=JPTaxi
DB_USER=postgres
DB_PASS=your_password
JWT_SECRET=your_super_secret_key_here
Bước 4: Tạo CSDL PostgreSQL và chạy script trong /database (DB.sql rồi DB_data.sql nếu cần).
Bước 5: Khởi chạy dev
Bash
npm run start:dev
# production build: npm run build && npm run start:prod

6. Thiết lập Frontend
Frontend là trang tĩnh: mở trực tiếp file .html trong /frontend hoặc phục vụ bằng bất kỳ static server nào (ví dụ VS Code Live Server).
URL API mặc định: http://localhost:3000/api (có thể ghi đè bằng window.JP_TAXI_API_BASE trước khi load js/api-config.js).
Đăng nhập quản trị (sau khi seed DB_data): POST /api/admin/login — tài khoản admin1 / admin123, mod1 / mod123 (chỉ môi trường dev).

7. Thiết lập Database
Option 1: Dùng Docker (Khuyến nghị)
Tạo file docker-compose.yml ở root project (nếu chưa có):
YAML
version: '3.8'
services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: jptaxi_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
Chạy:
Bash
docker-compose up -d db
Option 2: Cài trực tiếp
PostgreSQL: https://www.postgresql.org/download/
Tạo database jptaxi_dev
Chạy script trong thư mục /database.

8. Chạy Toàn bộ Project
Cách 1 (Manual):
Khởi động PostgreSQL và import /database.
Khởi động Backend: cd backend && npm run start:dev
Mở frontend: mở frontend/login.html (hoặc dùng Live Server trỏ vào thư mục frontend).

Cách 2 (Docker Compose - sau này):
Bash
docker-compose up --build

9. Công cụ hỗ trợ phát triển
VS Code Extensions khuyến nghị:
ESLint
Prettier
Docker
GitLens
Thunder Client / REST Client
Japanese Language Pack (nếu cần)
Quy tắc commit: Sử dụng Conventional Commits (feat:, fix:, docs:, v.v.)

10. Troubleshooting thường gặp
Port already in use: Kill process đang dùng port (npx kill-port 5000)
Database connection refused: Kiểm tra Docker có chạy không, hoặc credential trong .env
Module not found: Chạy npm install lại
Permission error: Chạy với quyền Administrator (Windows) hoặc sudo (macOS/Linux)

11. Lưu ý quan trọng
Không commit file .env chứa secret lên Git.
Luôn pull code mới nhất trước khi làm việc: git pull origin main
Báo cáo bug hoặc góp ý vào Issues trên GitHub.

Tài liệu này sẽ được cập nhật khi project có thay đổi về tech stack cụ thể (ví dụ: xác định rõ framework backend/frontend).
Người soạn: [Phạm Thanh Hải]
Ngày soạn: 10/05/2026

